<Window x:Class="StudentManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StudentManager"
        mc:Ignorable="d"
        Title="SQLite Demo" Height="350" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation = "Horizontal" Margin="0 0 0 10">
            <TextBox Width="200" x:Name="InputName" Margin="0 0 0 10"/>
            <Button Content="Add"
                    Width="150" Click="Add_Click"
                    />
            
        </StackPanel>
        <Button Content="Delete" Grid.Row="1" Width="150" Margin="0 0 0 10" Click="Delete_Click"/>
        <DataGrid Grid.Row="2" x:Name="DataGridPeople"/>

    </Grid>
</Window>
